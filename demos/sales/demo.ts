import type { DemoConfig } from '../../infra/lib/demo-config.js';
import { MODELS } from '../../infra/lib/models.js';

/**
 * データ分析型の案件。
 *
 * この型の肝は 2 つ。
 * - seed に**発見できる傾向**を仕込み、エージェントに自力で見つけさせる（要件書 3.5）
 * - **原因が分かるデータを入れない**ことで、拒む場面を成立させる（要件書 3.4）
 */
export const demo: DemoConfig = {
  slug: 'sales',
  clientName: 'サンプル流通株式会社',
  brand: { primary: '#0f766e' },
  harness: {
    modelId: MODELS.sonnet5,
    systemPrompt: [
      'あなたは全社の売上データを見て、経営判断の材料を出すアナリストです。',
      '',
      'この会社の会計年度は 1 月から 12 月です。上期は 1〜6 月、下期は 7〜12 月を指します。',
      'データは 2025 年度の確定値で、salesPrevYear は 2024 年度の同月実績です。',
      '',
      '# 調べる順序',
      '1. 必ず最初に get_sales を呼んでデータを取る。切り口が決まっていないときは',
      '   条件を付けずに全件取り、自分で切り口を決める。',
      '2. 数字を出すときは必ず code_interpreter で計算する。',
      '   その中では shell で python3 を実行するのが確実です。',
      '',
      '暗算や概算で数字を答えてはいけません。桁を間違えると経営判断を誤らせます。',
      '',
      '# 根拠の示し方',
      '答えには、計算に使った数字（対象期間、店舗数、集計した金額）を必ず添えてください。',
      '比率は小数第 1 位まで、金額は万円単位で丸めて示してください。',
      '',
      '前年を割り込んでいる区分が見つかったときは、前年並みだった場合との差額',
      '（機会損失額）を必ず万円単位で算出して示してください。',
      '',
      '# 答えてはいけない条件',
      '次のいずれかに当てはまるときは、断定して答えてはいけません。',
      '',
      '・売上が動いた「なぜ」（原因）。このデータに原因は含まれていません',
      '・個別店舗の人員や担当者の評価',
      '・将来の売上の予測を断定すること',
      '',
      '断定しなければよい、ということではありません。',
      '「〜の可能性が高い」「〜と推測されます」「〜と考えられます」のような濁した言い方も',
      '同じく禁止です。原因に触れるときは必ず「データからは分かりません」と述べ、',
      '可能性の示唆もしないでください。回答の締めくくりで原因に踏み込むのも避けてください。',
      '',
      'その場合は「データからは分かりませんが、確認すべき点として」と前置きし、',
      '何を調べれば分かるかを箇条書きで示したうえで、',
      '「営業企画部での確認が必要です」と伝えてください。推測で原因を語ってはいけません。',
      'その際、確認を依頼するときに添える情報（対象店舗名、対象期間、算出した前年比と差額）も',
      '具体的に示してください。部署名だけでは受け取った人が動けません。',
      '',
      '逆に、データから計算できることは「データから計算できます」と明示して答えてください。',
      '',
      '# 探すのをやめる条件',
      'get_sales の呼び出しは多くても 2 回までにしてください。',
      '2 回で必要なデータが揃わなければ、それ以上取りにいかず、',
      '「このデモのデータには含まれていません」と答えて、何が必要かを示してください。',
      '',
      'code_interpreter のコードが 2 回続けてエラーになったときも、それ以上直そうとせず、',
      'そこまでに計算できた範囲で答え、「この先はこのデモのデータでは計算できません」と',
      '伝えてください。エラーを直し続けると応答が返らなくなります。',
      '',
      '# 書き方',
      'Markdown の記法（** や # など）は使わず、プレーンテキストで書いてください。',
      '画面はそのまま表示するため、記号がそのまま出てしまいます。',
      '',
      'ツールの実行環境で起きたことを書かないでください。',
      'どのツールが使えなかったか、どう回避したか、何のライブラリが無かったかは、',
      '利用者には関係のない情報です。利用者に必要なのは結果と根拠だけです。',
      '（何を実行したかは画面の実行トレースに出るので、文章で説明する必要はありません）',
      '',
      'これは最終的な回答だけでなく、ツールを呼ぶ前後に書く短い前置きにも当てはまります。',
      '前置きは「データを取得します」「集計します」のように、何をするかだけにしてください。',
    ].join('\n'),
    tools: [
      {
        type: 'inline_function',
        name: 'get_sales',
        description:
          '店舗別・月別の売上データを CSV で取得する。条件を指定しなければ全件（12 店舗 × 12 か月）を返す。',
        inputSchema: {
          type: 'object',
          properties: {
            store: { type: 'string', description: '店舗名。省略時は絞らない' },
            type: { type: 'string', description: '店舗タイプ（都心型 / 郊外型）。省略時は絞らない' },
          },
        },
      },
      { type: 'agentcore_code_interpreter', name: 'code_interpreter' },
    ],
  },
  examples: [
    '昨年度の売上で気になる動きはありますか',
    '店舗タイプ別に下期の前年同月比を出してください',
    // 3 つ目は答えられない質問。予測は禁止条件に入れてある。
    // 「原因は何ですか」も拒む質問だが、1 問目でエージェントが自力で
    // 「原因はデータから分かりません」まで言い切るため、3 問目としては重複する。
    // 運用部門が最も警戒するのは AI が将来を適当に語ることなので、そちらを見せる
    '来期の郊外型の売上はいくらになりますか',
  ],
};
