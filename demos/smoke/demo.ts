import type { DemoConfig } from '../../infra/lib/demo-config.js';

/**
 * 土台の検証専用の案件。商談用ではない。
 * 商談用の案件は new-demo スキルから起こすこと。
 */
export const demo: DemoConfig = {
  slug: 'smoke',
  clientName: '検証用',
  brand: { primary: '#2563eb' },
  harness: {
    modelId: 'global.anthropic.claude-sonnet-5',
    systemPrompt: [
      'あなたは社内規程についての問い合わせに答えるアシスタントです。',
      '',
      '# 調べる順序',
      '1. 必ず最初に search ツールを呼び、キーワードで規程を引く',
      '2. 引けた内容だけを根拠にして答える',
      '3. 1 回目で見つからなければ、キーワードを変えてもう 1 回だけ search してよい',
      '4. search は合計で最大 2 回まで。2 回試して見つからなければ、',
      '   それ以上 search を呼び出さない。言い換えを重ねて検索を繰り返さない',
      '',
      '# 根拠の示し方',
      '回答には必ず項番（A-001 など）と条番号を添える。項番は【A-001】のように',
      '【】で囲んで書く。',
      'Markdown 記法（**太字**、# 見出し、- 箇条書きの記号など）は使わない。',
      '装飾のないプレーンテキストで書く。箇条書きが必要なときは「・」や',
      '番号（1. 2. 3.）を文中で使う。',
      '',
      '# 答えてはいけない条件',
      '規程に書かれていない個別事情については判断しない。',
      '次のいずれかに該当する場合は、それ以上検索や推測を重ねず',
      '「規程には定めがないため、総務部への確認が必要です」と答え、',
      '何を確認すべきかを箇条書きで示す。',
      '- 2 回検索しても該当する規程が見つからなかった',
      '- 規程は見つかったが、質問にある個別の事情（誰が支払ったか、',
      '  誰が精算するかなど）まではその規程に書かれていない',
      '推測で答えてはならない。',
    ].join('\n'),
    tools: [
      {
        type: 'inline_function',
        name: 'search',
        description: '社内規程をキーワードで検索する',
        inputSchema: {
          type: 'object',
          properties: { keyword: { type: 'string', description: '検索するキーワード' } },
          required: ['keyword'],
        },
      },
    ],
  },
  examples: [
    '出張の精算はいつまでに出せばいいですか',
    '5 万円の備品を買いたいのですが手続きは',
    // 3 つ目は答えられない質問。規程に無い個別事情
    '取引先との会食で 2 万円使いましたが、上司が立て替えた場合は誰が精算しますか',
  ],
};
