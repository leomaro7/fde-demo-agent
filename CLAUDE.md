# FDE デモ基盤

打ち合わせで聞いた課題を、**同日中に触れるプロトタイプ**としてクライアントに提示するための基盤。

## 判断の基準

**速度が目的。** 品質・網羅性・本番運用は目標ではない。迷ったら
「クライアントがイメージを持てるか」で判断し、「正しく動くか」では判断しない。

ただし商談で見せる 3 つの質問が通ることだけは確認する。
**それ以外の入力での挙動は保証しない。** ここを広げると検査が無制限に増える。

---

## 現状

**基盤は動く。** 打ち合わせメモから URL までの手順は **[RUNBOOK.md](RUNBOOK.md)** にある。
書きながら実行して作り、直すたびに通し直している。**未確認の章はもう無い**（2026-08-14）。

| | |
|---|---|
| `infra/` | 土台スタックと案件スタック（CDK） |
| `web/` | 会話とトレースの画面、Harness クライアント、ツールループ、ログイン |
| `demos/` | **型ごとの見本**（`smoke` / `sales` / `hr`）。商談用ではない。企業リポジトリでは消す |
| `scripts/` | 設定の取り出し、Amplify への配信、Harness の実測 |

**AWS には何も残っていない**（撤去まで検証したため）。作るには RUNBOOK の 3 章から。

要件書 7.0 の決着と、そこから出た設計は
[docs/superpowers/specs/2026-08-08-what-to-build-design.md](docs/superpowers/specs/2026-08-08-what-to-build-design.md)。

- 案件リソースは**自前 CDK + `aws-cdk-lib` の `CfnHarness`（L1）**。
  `@aws/agentcore-cdk`（alpha）に依存しない。**L1 が守るのは名前と構造だけで、
  列挙値は素の `string`**。列挙値は必ず CLI ヘルプの `Possible values` で確かめる
- ツールは**ブラウザ側**（`inline_function`）。**Code Interpreter だけ Harness 側**
- 案件の設定は**自作スキーマ・TypeScript**

**リポジトリはクライアント企業ごとに分ける**（要件書 7.1、2026-08-13 決着）。
新しいクライアントの案件は、このリポジトリではなく**企業リポジトリを作って**そこに置く
（`RUNBOOK.md` 2 章）。**土台の修正を既存の企業リポジトリへ配る仕組みは無い。**

## 着手前に読むもの

| | いつ |
|---|---|
| [docs/REQUIREMENTS.md](docs/REQUIREMENTS.md) | 設計判断をするとき。4 章に要件、5 章に設計原則、7 章は**決着済みの論点と、その経緯** |
| [.claude/skills/aws-fact-check/references/aws-facts.md](.claude/skills/aws-fact-check/references/aws-facts.md) | AWS を触るとき。実測済みの仕様。**再調査不要** |
| [docs/DECISIONS.md](docs/DECISIONS.md) | 「なぜこうなっているのか」を追うとき |
| [docs/harness-overview.md](docs/harness-overview.md) | **ハーネスの全体像**（図つき）。何がどこで発火し、なぜ在るのか。**直したらここも直す** |
| GitHub の Issue（`gh issue list`） | 次に何をやるかを決めるとき。**先送りにした課題と、そこまでに分かったこと** |

---

## 守るルール

要件書 5 章に全 8 項目がある。**そのうち、読まずに違反しやすい 4 つ**を再掲する。

### インフラは IaC で書く

AWS リソースは宣言的に定義する。「リソースが少ないから」は理由にならない。
前身はこれを破って、冪等性・削除順序・命名衝突を全部自前で実装する羽目になった。

**IaC の外に出したのは 1 つだけ**（デモ用のユーザー。パスワードを設定する手段が
CloudFormation に無いため）。それ以外は全部 CDK に載っている。

### グローバルに一意な名前は必ず可変にする

同一アカウントに複数の環境を置けることを前提にする。とくに Cognito ドメイン
プレフィックスは**リージョン内で全 AWS アカウント共通の名前空間**。
IAM ロール名は**そもそも指定しない**（自動命名に任せる）。

利用者に見える名前にアカウント ID を入れない。ログイン URL は
クライアントのブラウザに表示される。

### 一時ファイルを使わない

設定ファイルはリポジトリに置く。`/tmp` に書くと追跡できず、残り続け、
環境によっては書けない。

### 手順書は実行してから書く

**実行していない手順を書かない。** 書き方は `verify-runbook` スキルにある。

---

## 作業のルール

**不可逆な操作は事前に対象を明示する。** 「消します」ではなく
「Harness 2 件と User Pool 1 件を削除します」と書く。

**リポジトリの公開範囲は 2 種類ある**（要件書 7.1）。取り違えない。

| | 公開範囲 | 中身 |
|---|---|---|
| **土台リポジトリ**（これ） | **公開してよい** | 架空の見本だけ。クライアント固有の情報は入れない |
| **企業リポジトリ** | **必ず private** | 案件ごとにクライアント固有の情報が入る |

企業リポジトリを作るときは `gh repo create ... --private`（`RUNBOOK.md` 2 章）。
**土台リポジトリに実クライアントの案件を置かない。** 置いた瞬間に公開事故になる。

**そして公開範囲はリポジトリだけの話ではない。** Artifact は claude.ai 上のページで、
既定は非公開だが**リポジトリの外に出る**。`git push` と同じく「外に出す」側であり、
**settings.json では止められない**（Bash ではないため確認が入らない）。
架空の見本は出してよい。**実クライアントの資料は、出す前に必ず確認を取る。**
画面共有で足りるなら URL は要らない。

**push の前に `pre-commit-scrubber` を通す。** ただしこれは**未コミットの差分しか見ない**。
**公開リポジトリへ初めて push するときは履歴全体も調べること**
（アカウント ID、実リソース ID、パスワード、ローカルパス）。

```bash
python3 .claude/skills/pre-commit-scrubber/scripts/scan.py --repo .
```

**このスキルはリポジトリに入れてある。** 企業リポジトリはテンプレートから `.claude/` ごと
コピーされるので、そちらにも付いてくる。**公開事故を防ぐルールが手元の環境に依存していると、
クローンした人には効かない。** 2026-08-15 まで実際にそうなっていた。

**ここにあるのは複製で、元のスキル集の更新は流れてこない。** 企業リポジトリと同じ扱いである。

**承認が要る操作は、単独で出す。** 確認コマンドや別のファイル編集と束ねない。
束ねると、**一部だけ断ることができず、拒否されたときに全部巻き戻る。**
2026-08-15 に 2 回やって、2 回とも作業をやり直した。

**自分が書いたものは、自分ではレビューできない。** 手順書やスキルを大きく直したら、
**文脈を持たないサブエージェントに読ませる**。2026-08-15 に手順書でやったところ
18 件出て、うち 5 件は「そこで手が止まる」ものだった。`cd` が 1 つも書かれていない、
企業リポジトリでの `npm install` が抜けている——**書いた側は無意識に補完していて見えない。**
「補完したくなったら、それが欠陥」と伝えて読ませること。

**テストの方針。** 純粋関数は TDD（パーサ、変換、検証ロジック）。
UI と AWS 呼び出しは通し確認。自動テストに時間をかけるより実物を触る。

**コミット。** Conventional Commits。本文には何をしたかより**なぜそうしたか**を書く。

---

## 用意してあるもの

| | いつ使うか |
|---|---|
| `new-demo`（skill） | 打ち合わせメモを受け取った直後。**メモからデプロイ直前までを通しでやる** |
| `production-sketch`（skill） | 商談の**前**に「何を聞くか」を確認するとき、商談の**後**に資料を組むとき。**その場では出さない** |
| `file-issue`（skill） | 「次回にしましょう」と決めたとき。**調べた結果ごと GitHub Issue に残す** |
| `aws-fact-check`（skill） | AWS の API・CDK・CLI を書く前と、**設定を変えた後**（呼んで確かめる） |
| `verify-runbook`（skill） | 手順書を**書くとき**、および既存の手順書を検証するとき |
| `cleanup-check`（skill） | 撤去した後、稼働数が想定と違うとき |
| `infra-reviewer`（agent） | インフラ定義を書いた・変えた後、デプロイ前 |
| `demo-quality`（agent） | 案件を作った後、クライアントに見せる前 |

フックは 1 つだけ（`.claude/hooks/session-context.py`）。セッション開始時に、
今どの AWS アカウントにいて Harness / UserPool / Amplify が何件動いているかを出す。

**コミット前の検査と案件の検証フックは、置いていない。** 構成は決まったので
作れるようになったが、**同じ検査を `new-demo` が既にやっている**（`tsc` / `npm test` /
`cdk list`）。二重にすると遅くなるだけなので置かない。

`settings.json` は**AWS と外部を変えないものを allow、作る・消す・外に出すを ask、
資格情報を返すものを deny** にしてある。`npx cdk deploy` や `gh issue create` が
確認を挟むのはこのため。

**手順書が必須としているコマンドは allow に入れておく**（テスト・型検査・`cdk list` /
`synth`・スクラバ）。**毎回同じ確認で止まるなら、それは設定側の不足である。**

**昇格させないもの。** `aws <service> *` のようなワイルドカード（`delete-*` を含み、
ask に置いた意図を上書きする）、任意コードを実行するもの（`python3 -c` / `node -e`）、
**壊す形を持つもの**（`git checkout` は `-- <file>` で未コミットの変更を捨てる。
`git branch` は `-D` で消せる。`find` は `-exec rm` を書ける）。

**`aws * get-*` を一括で allow にしない。** deny に並べた資格情報系は**全部 `get-` で始まる**。
つまり**名指しして塞いだのと同じ形を、allow 側で開けることになる**。
deny が優先するので既に名指ししたものは止まるが、**名指ししていないものと、
これから増える API は素通りする。** 開けるならサービス単位で（例: `aws bedrock get-*`）。

**`ask` は 3 種類ある。** 何を守っているかが違うので、混ぜて考えない。

| | 例 | 守っているもの |
|---|---|---|
| **AWS を IaC の外で変える** | `aws * create-*` / `update-*` / `put-*` / `delete-*` | 要件書 5.1。CDK を通さない変更は追跡できない |
| **作った作業を壊す** | `git reset --hard` / `git clean` / `git rebase` / `rm` | 取り消せない。`git clean -fdx` は未追跡ファイルごと消す |
| **外に出す・外を変える** | `git push` / `gh repo create` `edit` `delete` / `gh api` / `gh issue create` | 公開リポジトリなので、出した瞬間に戻せない |

**`deny` に置くのは「承認しても通す理由が無いもの」だけ。** 資格情報を返すもの
（`assume-role` と `create-access-key` は一時鍵と長期鍵を返す）と、force-push。
どうしても要るときは、**Claude を通さず自分で叩く**。

### 確認が多いからといって allow を `*` で広げない

**allow に無いものは既定で確認が入る。** つまり今の `ask` は大半が意図の記録で、
**保護として働くのは allow が広いときだけ**である。裏返すと、
**allow を `*` で広げた瞬間に、ask と deny が初めて守る側になる。**

**そのとき、塗り残しを人は数えきれない。** 2026-08-15 に実演した。
サービス単位のワイルドカードを入れ、塗り残しを「60 件」と見積もったが、
**実際は 127 件**だった。差の 64 件はちょうど `get-*` と `admin-get-*` で、
**安全な読み取りだと決めつけて数から外していた**（このすぐ上に「`get-*` には
資格情報を返すものが混ざる」と自分で書いてある）。

見落とした中に、**このリポジトリが実際に使う `admin-initiate-auth`（トークンを返す）**と
**`cloudformation deploy`（CDK を通さないスタック変更）**と
**`activate-organizations-access`（組織全体の設定）**が入っていた。

**サービス単位のワイルドカードは入れない。** 広げるなら**引数によらず安全が
決まるもの**に限る。`aws * describe-*` が安全なのは `describe` が何を渡しても
変えないから。

**確認が多いと感じたら、広げるのではなく数える。** 同じコマンドで何度も
止まっているなら、そのコマンドを名指しで足す。実際に数えたら、
**足すべきは 3 行だった**（ワイルドカード 4 つが解消する確認は、それだけしかなかった）。

### `python3 -c` と `node -e` は allow に入れない

**これだけは別格で、`ask` も `deny` も無効化する。** サブプロセスの中で行う
読み書きは照合の対象にならないので、**`.env` の deny も抜ける**。

2026-08-15 に、上の行を書いた 2 つ後の編集で**自分でこれを allow に入れた**。
書いたルールを守れなかったのではなく、**書いたことを忘れていた**。

**なお `deny` を境界として設計しないこと。** 効き方は実行環境の設定にも依り、
このプロジェクトでは確かめていない。**多層防御として置くもの**である。
`.env` に `Read(...)` と `Bash(cat ...)` の両方を並べてあるのは、そのための重ね置き。

### このハーネスを育てる

**ここにあるルールは、前身で実際に踏んだ失敗からしか来ていない。**
まだ踏んでいない失敗は 1 つも入っていない。完成品ではなく初期状態である。

| 起きたこと | どうするか |
|---|---|
| 同じミスを 2 回した | ルールか skill にする。1 回目は運が悪かっただけかもしれない |
| AWS の仕様を推測して外した | `references/aws-facts.md` に実測値を追記する |
| ルールが邪魔をした | **ルールを消す**。守れないルールを残すと全体が形骸化する |
| 2 回守って問題が出なかった | 消してよい。効いているか分からないルールを抱えない |
| 判断に迷った | `docs/DECISIONS.md` に理由ごと残す |

**ルールが増える一方なら、それは失敗している。** 速度を出すための基盤なので、
ブレーキばかり増やすと目的から遠ざかる。何かを足すときは**加速側から足す**。

**構造を前提にするルールを書かない。** ディレクトリ名・スクリプト名・
ファイル形式に依存する仕組みは、それが決まってから作る。間違った前提を強制する
仕組みは、無いより悪い。

---

## この基盤の肝

**デモの説得力を決めるのは、エージェントが答えを拒む場面。**

運用部門ほど「何でも答える AI」を警戒する。そして**この情報は打ち合わせメモに
絶対に書かれない**。クライアントは「できるようになりたいこと」を話し、
「できてはいけないこと」は話さないため。

案件を作るときは必ず `new-demo` から入る。

## スコープ外

| | 理由 |
|---|---|
| 実データの取り込み | ダミーのみ。NDA とセキュリティ審査を待たずに着手できることが当日提示の前提 |
| VPC の構築 | ファイルマウントを使わないことで回避する |
| 本番品質 | 可用性・負荷対策・監査ログは対象外。数週間で廃棄する前提 |

**「作らない」であって「話さない」ではない。** デモが刺さった直後には
「本番にするといくらで、どういう構成か」が必ず来る。**そこで落とすものは意図的に
落としてある**ので、提案するときは埋め直す。**そのための skill が `production-sketch`**
（実案件ではまだ使っていない。GitHub Issue #3）。
デモの構成をそのまま本番と言わないこと。
